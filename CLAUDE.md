# picasso — 이 저장소에서 일하는 방법

이기종 로봇과 공장 상위 시스템 사이의 **계약·미들웨어 PoC**. 16 모듈, Kotlin/Gradle, JVM 21.
무엇이 어디 있는지는 [`README.md`](README.md) 의 문서 지도가 답한다.

> **이 파일에 없는 것:** 이 프로젝트를 왜 이 도메인으로 골랐는지, 어떤 1차 출처를 대조하는지는
> **저장소 밖 프로젝트 메모리**에 있다. 저장소에 안 들인다.

## 명령

```bash
./gradlew build                                    # 전체 (테스트 1,486)
./gradlew :gate:test                               # 게이트만
./gradlew :gate:test --tests '*DocumentClaimsTest*' # 문서↔코드 대조만
```

## 규율 — 이 저장소가 물려서 배운 것

1. **결함 주입으로 시험을 시험한다.** 새 검사를 만들면 그것을 깨는 변경을 넣어 **겨냥한 단언이 잡는지**까지 본다.
   완벽한 결과(전부 잡힘)도 신호다 — 대개 러너가 안 돈 것이다.
2. **못 잡으면 주입부터 의심한다.** 순서: ① 시험이 돌았나 ② 주입이 실제로 들어갔나 ③ 등가 변이인가 ④ 시험의 구멍.
3. **잡혔다고 나올 때도 *그것을 잡은 것이 시험인가* 를 본다.** 안 도는 러너의 exit 1 과 빨간 시험의 exit 1 은
   같은 숫자다. 판정은 종료 코드가 아니라 **XML 결과의 실패한 시험 이름**으로 하고, 주입 전에 러너를 맨몸으로
   한 번 돌려 *돈 시험 수 > 0 · 실패 0* 을 확인한다.
4. **시험이 저장소 파일을 읽으면 `gate/build.gradle.kts` 의 `repoInputs` 에 선언돼 있어야 한다.**
   `gate/src/test/.../Repo.kt` 가 그 문이고, 선언 안 된 파일을 읽으면 **그 자리에서 던진다.**
   같은 구멍에 여섯 번 물렸다 — 선언이 없으면 태스크가 `UP-TO-DATE` 로 넘어가 **시험이 안 돌고 빌드는 초록**이다.
   대가로 아무 소스나 바뀌면 `:gate:test` 가 다시 돈다. **안 도는 검사보다 늦게 도는 검사가 낫다.**
5. **근거 등급이 낮으면 `NO` 가 아니라 `UNKNOWN` 이다.** 낮은 등급 위에 판정을 얹었다가 한 기종의 판정 넷이
   통째로 뒤집힌 적이 있다. 부재를 적으려면 **벤더 1차 표면을 다 세고**(스펙·문서·클라이언트 코드·예제)
   조사 범위를 함께 적는다. 부재 판정은 **벤더의 낱말로 다시 물어야** 한다(`nogo` vs `keep-out`).

## 문서는 주장하고, 검사가 그 주장을 지킨다

`DocumentClaimsTest` 가 문서와 코드를 대조한다. 문서를 고칠 때 이 대조를 같이 본다.

| 문서 | 검사가 지키는 것 |
|---|---|
| [`docs/seams.md`](docs/seams.md) | 색인의 모든 줄이 읽히고, 그 인터페이스가 실재하고, 색인과 본문이 같은 집합 |
| [`docs/contract.md`](docs/contract.md) | 담보마다 **그것을 지키는 시험**을 대고, 그 이름이 코드에 실재 |
| [`docs/isa95.md`](docs/isa95.md) | 표준 타입 이름이 코드와 일치, **근거 등급이 계약 proto 로 안 샘** |
| [`docs/commissioning.md`](docs/commissioning.md) | 설정을 바꾸는 문(`/operations`·`/ingest`)이 빠짐없이 적힘 |
| `README.md` · 모듈 `README.md` | 모듈 수·검사 수·어댑터 수·계약 semver·문서 링크 |

**시험이 없는 담보는 담보가 아니라 희망이다.** 새 담보를 문서에 적으면 시험 이름을 같이 적는다.

## 계약에 무엇을 들이나

관문 둘을 **다** 지나야 한다 — **소비자가 있어야 하고**(ADR 9), **발명이 아니어야 한다**(ADR 36: 상류나 벤더 중
한쪽이 이미 가진 것만). 자세한 것은 [`docs/contract.md`](docs/contract.md), 결정은 [`docs/adr/`](docs/adr/README.md).

**벤더 원문은 저장소에 안 들인다** — 이름과 sha256 만 `adapter-*/src/test/resources/vendor-manifest.txt` 에 남긴다.
**벤더 이름은 `picasso/src/main` 에 못 들어간다**(게이트 7번, 주석이라도).

## 작업 습관

- 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
- **`main` 직접 커밋 금지 — feature 브랜치 → PR.** 이 저장소는 리모트가 없어 지금까지 main 직행이었다.
- 파일을 고칠 때 **CRLF/LF 를 보존한다**. 셸 heredoc 은 `\` 를 `\` 로 접으므로 Kotlin 정규식·파이썬 앵커가
  깨진다 — Kotlin raw string(`"""..."""`) 을 쓰거나 파이썬에서 `chr(92)` 로 만든다.
- 열린 항목은 [`docs/limits.md`](docs/limits.md), 운행 기록은 설계 문서 `§15`(항목을 지우지 않고 정정을 덧쓴다).
