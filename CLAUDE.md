# picasso — 개발 및 기여 가이드

이기종 모바일 로봇과 공장 상위 시스템(MES·WMS) 간의 **표준 인터페이스 계약 및 미들웨어 PoC** 프로젝트입니다. (16개 모듈, Kotlin, Gradle, JDK 21 기반)
전체 시스템 구조 및 상세 문서는 [`README.md`](README.md)의 문서 지도를 참조하십시오.

> **참고:** 본 프로젝트의 도메인 선정 배경 및 상세 1차 벤더 대조 자료는 외부 프로젝트 메모리에 보관하며, 본 저장소에는 포함하지 않습니다.

## 1. 빌드 및 테스트 명령

```bash
# 전체 빌드 및 테스트 실행 (총 1,579개 테스트)
./gradlew build

# 아키텍처 및 품질 게이트 검증만 실행
./gradlew :gate:test

# 문서와 코드 간의 정합성 대조 검증만 실행
./gradlew :gate:test --tests '*DocumentClaimsTest*'
```

## 2. 핵심 엔지니어링 및 테스트 원칙

1. **결함 주입(Mutation/Fault Injection)을 통한 검증의 유효성 입증**:
   - 신규 검증 로직 작성 시, 의도적으로 결함을 주입하여 해당 단언(Assertion)이 결함을 정확히 탐지하는지 검증합니다.
   - 모든 검증이 예외 없이 통과할 경우, 테스트 러너가 정상 실행되지 않았을 가능성을 우선 점검합니다.
2. **테스트 실패 시 원인 추적 순서**:
   - ① 테스트 실행 여부 확인 → ② 주입된 변경사항의 실제 반영 여부 확인 → ③ 등가 변이(Equivalent Mutation) 여부 검토 → ④ 테스트 로직의 누락 구간 분석.
3. **테스트 실패 판정 기준**:
   - 단순히 프로세스 종료 코드(Exit Code 1)에 의존하지 않고, **테스트 결과 리포트(XML)에 명시된 실패 테스트 케이스 이름**으로 실제 탐지 여부를 판정합니다.
4. **저장소 파일 참조 시 Gradle 입력 선언 의무화**:
   - 테스트 코드가 저장소 내 파일을 직접 읽는 경우, `gate/build.gradle.kts`의 `repoInputs`에 해당 파일/경로가 명시적으로 선언되어 있어야 합니다.
   - 선언되지 않은 파일을 참조할 경우 `Repo.kt` 게이트웨이에서 즉시 예외를 발생시킵니다.
   - 선언이 누락되면 Gradle 태스크가 `UP-TO-DATE`로 처리되어 실제 변경사항이 검증되지 않는 허위 통과가 발생할 수 있습니다.
5. **근거 미확인 시 `UNKNOWN` 명시 (3값 논리 체계)**:
   - 벤더 문서 등 1차 출처에서 기능 지원 여부가 명확히 확인되지 않은 경우, 임의로 `NO`로 단정하지 않고 `UNKNOWN`으로 분류합니다.
   - 미지원(`NO`)으로 판정하려면 공식 스펙, API 문서, 클라이언트 라이브러리, 예제 코드 등 벤더 1차 자료의 전수 조사를 거쳐야 하며, 조사 범위(`survey_scope`)를 함께 명시해야 합니다.

## 3. 문서-코드 정합성 검증 (`DocumentClaimsTest`)

본 저장소의 문서는 단순 텍스트가 아니며, `DocumentClaimsTest`를 통해 코드 및 실제 메타데이터와의 정합성을 자동으로 검증합니다. 문서를 수정할 때는 반드시 해당 검증을 통과해야 합니다.

| 대상 문서 | 자동화 검증 항목 |
|---|---|
| [`docs/seams.md`](docs/seams.md) | 색인의 모든 항목 파싱 가능 여부, 실제 인터페이스 타입의 존재 여부, 색인과 본문 집합의 완전 일치 |
| [`docs/contract.md`](docs/contract.md) | 각 계약 담보 사항과 1:1 매핑된 검증 테스트 클래스 및 메서드의 실재 여부 |
| [`docs/isa95.md`](docs/isa95.md) | 표준 타입 식별자와 코드 간 일치 여부, 계약 proto로의 근거 등급 누출 차단 여부 |
| [`docs/commissioning.md`](docs/commissioning.md) | 설정 변경 엔드포인트(`/operations`, `/ingest` 등)의 전수 명시 여부 |
| `README.md` · 모듈 `README.md` | 모듈 수, 검사 수, 어댑터 수, 계약 SemVer, 문서 링크 유효성 일치 여부 |

> **규칙:** 검증 테스트가 연결되지 않은 인터페이스 담보는 보증된 사양으로 간주하지 않습니다. 새로운 담보 사항을 문서화할 때는 반드시 이를 증명하는 테스트명을 함께 기재해야 합니다.

## 4. 인터페이스 계약(Contract) 포함 기준

신규 필드, RPC, 에러 코드는 다음 두 가지 게이트를 모두 통과해야 계약에 포함될 수 있습니다:
- **소비자 존재 원칙 (ADR 9)**: 실제 상위 시스템 또는 다운스트림에서 소비하는 명확한 사용처가 정의되어 있어야 합니다.
- **표준 및 기존 어휘 기반 (ADR 36)**: 상류 시스템 표준(ISA-95 등) 또는 로봇 벤더 표면 중 최소 한쪽에 이미 존재하는 개념이어야 하며, 자체적으로 임의 발명한 개념은 배제합니다.

상세 사양은 [`docs/contract.md`](docs/contract.md), 설계 의사결정은 [`docs/adr/`](docs/adr/README.md)를 참조하십시오.

### 아키텍처 격리 규칙
- **벤더 SDK 격리**: 저장소 내에 벤더 SDK 바이너리 및 소스코드를 포함하지 않으며, 심볼 명칭과 SHA-256 해시만 `adapter-*/src/test/resources/vendor-manifest.txt`에 기록하여 대조합니다.
- **벤더 의존성 누출 차단**: `picasso/src/main`을 비롯한 공용 모듈에는 특정 벤더/기종 식별자(주석 포함)가 포함될 수 없습니다. (CI 게이트 검사 7번 집행)

## 5. 작업 규칙 및 협업 수칙

- **커밋 메시지**: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` 트레일러 유지.
- **브랜치 전략**: `main` 브랜치 직접 커밋을 지양하고, Feature 브랜치 생성 후 PR을 통해 병합합니다.
- **줄바꿈(CRLF/LF) 보존**: 파일 수정 시 기존 줄바꿈 형식을 유지합니다. 정규식 처리 시 이스케이프 문자(`\`) 처리에 유의합니다.
- **한계점 및 히스토리 관리**: 미결 과제는 [`docs/limits.md`](docs/limits.md)에 기록하며, 설계 문서 `§15`의 변경 이력은 기존 항목을 삭제하지 않고 정정 내용을 누적 기록합니다.
